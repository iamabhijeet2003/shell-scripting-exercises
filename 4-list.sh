#!/bin/bash
# Shebang line: Indicates the path to the shell interpreter (in this case, bash)

# Script to list all files and directories in the current directory using the ls command

# Listing all files and directories in the current directory
ls
# ls command: Lists the files and directories in the current directory
